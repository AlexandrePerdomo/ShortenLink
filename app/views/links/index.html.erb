<p id="notice"><%= notice %></p>

<h1>Links</h1>

<h2>New link</h2>
<%= render 'form', link: @link %>

<table>

  <thead>
    <tr>
      <th colspan="1">Shorten URL</th>
      <th colspan="1">Default URL</th>
      <th colspan="1">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @links.each do |link| %>
      <tr>
        <td><%= link_to "#{request.base_url}/#{link.code}", link_show_path(link.code), target: :_blank %></td>
        <td><%= link_to "#{link.url}", "#{link.url}", target: :_blank %></td>
        <td>
          <%= link_to 'Supprimer', link, method: :delete, data: { confirm: 'Are you sure?' } %>
          <%= link_to 'Tracks', link_tracks_path(link), remote: true %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div id="chart"></div>